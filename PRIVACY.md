# 隐私说明（Privacy Policy）

**生效版本**：webbuddy v1.5.0　|　**最后更新**：2026-09-16

webbuddy 是纯本地运行的浏览器扩展。**没有开发者服务器、没有账号体系、没有遥测统计。**
全代码开源可审计，你随时可以自己核对下面每一条说法。

---

## 一句话总结

> 你浏览什么、选中什么、收藏什么标签，开发者一概看不到。
> 唯一会离开浏览器的数据，是你**主动点提问时**发出去的那段文字/截图，
> 而且它直接发往**你自己填写的 API 服务商**（如 DeepSeek、OpenAI、Moonshot、Qwen，或自建的 Ollama）。

---

## 一、数据去向

### 1. 会离开浏览器的（只有这一类）

| 数据 | 何时发送 | 发往何处 |
|---|---|---|
| 你选中的文字 | 点击划词气泡中的预设问题 / 右键「问 AI」时 | 你在设置页填写的 API Base URL |
| 当前页面正文（约前 8000 字）、标题、URL | 使用「解读当前页面」时 | 同上 |
| 你框选区域的截图 | 使用「截图问图」时 | 同上 |
| 你在侧边栏输入的问题 + 上述上下文 | 发送提问时 | 同上 |

**关键点**：

- 请求由扩展**直接**发给该 API，**不经过**任何中间服务器、代理或开发者的转发层。
- 换成自建服务（如本机 `http://localhost:11434/v1` 的 Ollama），数据就**完全不出本机**。
- 这些内容受你所选服务商自己的隐私政策约束，请自行查阅其条款。
- API Key 会作为 `Authorization` 头随请求发出（这是调用 API 的必需流程），且只发给你配置的那个域名。

### 2. 不会离开浏览器的

| 数据 | 存在哪里 | 说明 |
|---|---|---|
| API Key | `chrome.storage.local` | 仅存本机，不上传、不同步到 Google 账号 |
| Tab Dock 的标签分组 / 回收站 | `chrome.storage.local` | 纯本地，无「分享为网页」等需上传的功能 |
| 图片模式、免广告等开关 | `chrome.storage.local` | 纯本地 |
| 问答历史 | `chrome.storage.session` | **仅当前浏览器会话**，关闭浏览器即清空，不留痕 |

### 3. 明确不做的事

- ❌ 不收集任何使用统计、崩溃报告、埋点、遥测
- ❌ 不要求注册账号，不接触你的邮箱 / 手机号
- ❌ 不读取你的书签、历史记录、Cookie、密码
- ❌ 不上传你的浏览记录
- ❌ 不加载任何远程代码（MV3 要求 + 本项目的自主选择），`libs/` 下的 marked / DOMPurify / highlight.js 全部本地打包
- ❌ 无任何隐藏的网络请求

**可自行验证**：全仓库只有一处真正的 HTTP 请求，在 `common.js` 的 `fetch(url, ...)`，其 `url` 就是你设置的 Base URL。
（`background.js` 中另有一处 `fetch()`，对象来自 `chrome.tabs.captureVisibleTab()` 返回的 `data:` URL，用于本地裁剪截图，**不产生网络通信**。）

---

## 二、权限与用途

| 权限 | 为什么需要 | 会做什么 / 不会做什么 |
|---|---|---|
| `contextMenus` | 右键菜单入口 | 仅在网页上添加几项右键菜单 |
| `sidePanel` | 侧边栏问答界面 | 只在你主动打开时显示 |
| `storage` | 保存配置与 Tab Dock 数据 | 存本机；`session` 部分关浏览器即丢 |
| `activeTab` | 读取当前页上下文、执行截图 | 仅在**你主动提问/截图时**临时取用 |
| `scripting` | 注入图片控制与划词气泡脚本 | 只改页面显示（隐藏/缩略图片、插入气泡） |
| `tabs` | Tab Dock 收纳标签 | 读取标签标题/网址以收纳，恢复/关闭标签；**仅本地使用，不联网** |
| `host_permissions: <all_urls>` | 图片控制与划词气泡需在任意页面即时生效 | 安装时浏览器会提示「读取所有网站数据」，这是实现所必需；**不用于收集任何数据** |

> 说明：图片隐藏 / 缩略 / 免广告**不拦截网络请求**，只控制页面显示，因此也不会有「上传你看过哪些广告」的可能。

---

## 三、数据保留与删除

| 想删什么 | 怎么做 |
|---|---|
| 清空问答历史 | 关闭浏览器即自动清空（session 存储）；或点侧边栏「＋」新建会话 |
| 删除 API Key / 全部配置 | 设置页清空后保存；或在 `chrome://extensions` 删除扩展 |
| 删除 Tab Dock 数据 | Tab Dock 管理页 → Trash → 清空；或删除扩展 |
| 彻底清除全部 | 卸载扩展，Chrome 会一并清除其所有 `storage` 数据 |

导出：Tab Dock 支持导出为 JSON 文件，方便你自己备份或迁移。

---

## 四、第三方依赖

运行时依赖全部本地打包在 `libs/`，均为成熟开源库，不联网、不收集数据：

- `marked.min.js` — Markdown 渲染
- `purify.min.js`（DOMPurify）— HTML 净化，防 XSS
- `highlight.min.js` + `github.min.css`（highlight.js）— 代码高亮

---

## 五、变更与联系

本说明随版本更新，改动会体现在本文件与 Release 记录中。

发现与本文描述不符的行为？欢迎在仓库提 Issue —— 那是 bug，不是设计。

- 仓库：https://github.com/luxviat-tools/webbuddy
- 许可证：MIT（见 [LICENSE](./LICENSE)）
